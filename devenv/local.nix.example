# Local, machine/project-specific overrides — NOT committed (gitignored),
# but this file documents the pattern. Copy to devenv/local.nix.
#
# {
#   templateConfig.ncro.enable = true;
#   templateConfig.processes.enable = true;
#   templateConfig.toggles.wasm.enable = true;  # opt-in langs
#   templateConfig.toggles.python.enable = true;
#   templateConfig.languages.rust.rustflags = [ "-C target-cpu=native" ];
# }
